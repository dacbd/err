name: trigger deployment
on: 
  push:

      
jobs:
  environment: dev
  deploy:
    uses: ./.github/workflows/alt-triggered.yml
    with:
      deploy_ref: ${{ github.ref_name }}
    secrets: inherit
